(function(){
  let products=[];
  const FALLBACK_PACKS=[{amount:7249,price:2925},{amount:3663,price:1475},{amount:2227,price:885},{amount:759,price:295},{amount:335,price:145},{amount:203,price:90},{amount:66,price:30}];
  function normalize(v){return String(v||'').trim();}
  function cleanOrderName(v){return normalize(v).replace(/\s*[×xX]\s*\d+\s*$/,'').trim();}
  function buttonAmountFromName(name){if(window.YMK_BUTTON_PACKS?.buttonAmountFromName)return window.YMK_BUTTON_PACKS.buttonAmountFromName(name);const t=normalize(name),m=t.match(/(\d[\d,]*)\s*(?:กระดุม|ปุ่ม|buttons?)/i);if(m)return Number(m[1].replace(/,/g,''));const a=[...t.matchAll(/\d[\d,]*/g)];return a.length?Number(a[a.length-1][0].replace(/,/g,''))||0:0;}
  function bestPlan(target){if(window.YMK_BUTTON_PACKS?.bestPlan)return window.YMK_BUTTON_PACKS.bestPlan(target);target=Math.max(1,Math.floor(Number(target)||1));let best=null;FALLBACK_PACKS.forEach(pack=>{const qty=Math.max(1,Math.ceil(target/pack.amount)),received=pack.amount*qty,price=pack.price*qty,c={target,received,price,text:pack.amount+'×'+qty,packAmount:pack.amount,packQty:qty};if(!best||price<best.price||(price===best.price&&(received<best.received||(received===best.received&&qty<best.packQty))))best=c;});return best;}
  function findProduct(order){const item=cleanOrderName(order?.item),pack=cleanOrderName(order?.pack);return products.find(p=>{const name=normalize(p.name);return name&&(name===item||name===pack);})||null;}
  function patchCurrentOrder(){try{if(typeof lastOrder==='undefined'||!lastOrder)return;const p=findProduct(lastOrder);if(!p||!['skins','accessories'].includes(String(p.category||'')))return;const each=buttonAmountFromName(p.name);if(!each)return;const qty=Math.max(1,Math.floor(Number(lastOrder.quantity)||1)),target=each*qty,plan=bestPlan(target);if(!plan)return;const extra=plan.received-plan.target;lastOrder.requiredButtons=plan.target;lastOrder.receivedButtons=plan.received;lastOrder.packPlan=plan.text;lastOrder.buttonPack=plan.target;lastOrder.price=plan.price;lastOrder.pack='แพ็ก '+plan.text+' = '+plan.received.toLocaleString('th-TH')+' กระดุม'+(extra>0?' (เกิน '+extra.toLocaleString('th-TH')+')':'');}catch(e){console.warn('button pack patch failed',e);}}
  function loadProducts(){try{if(!window.firebase||!firebase.firestore)return setTimeout(loadProducts,250);if(!firebase.apps.length){if(!window.YUIMELLKUB_FIREBASE_CONFIG)return setTimeout(loadProducts,250);firebase.initializeApp(window.YUIMELLKUB_FIREBASE_CONFIG);}firebase.firestore().collection('products').onSnapshot(s=>{products=s.docs.map(d=>({id:d.id,...d.data()}));},e=>console.warn('product lookup for button pack failed',e));}catch(e){setTimeout(loadProducts,500);}}
  function install(){if(typeof window.saveOrderToDemoAdmin!=='function')return setTimeout(install,250);if(window.saveOrderToDemoAdmin.__ymkButtonPackWrapped)return;const original=window.saveOrderToDemoAdmin;async function wrapped(){patchCurrentOrder();return original.apply(this,arguments);}wrapped.__ymkButtonPackWrapped=true;window.saveOrderToDemoAdmin=wrapped;}
  loadProducts();install();
})();