(function(){
'use strict';
const MAX_IMAGES=30,MAX_SOURCE_BYTES=5*1024*1024,TOTAL_DATA_BUDGET=820000;
const style=document.createElement('style');style.textContent=`.ymk-fulfillment-paste{border:1px solid #efc8d7;border-radius:11px;padding:9px 12px;font-weight:800;cursor:pointer;background:#fff0f6;color:#92566e}.ymk-fulfillment-paste-zone{margin-top:8px;padding:10px 12px;border:1px dashed #e8aec6;border-radius:11px;color:#9a687c;background:#fffafd;font-size:11px;line-height:1.5;outline:none}`;document.head.appendChild(style);
function dataUrlToFile(dataUrl,name){const p=String(dataUrl||'').split(','),m=(p[0]||'').match(/data:([^;]+);base64/);if(!m)return null;try{const bin=atob(p[1]||''),u8=new Uint8Array(bin.length);for(let i=0;i<bin.length;i++)u8[i]=bin.charCodeAt(i);return new File([u8],name||'saved-slip.jpg',{type:m[1]||'image/jpeg'});}catch(e){return null;}}
function compressImage(file,targetBytes){return new Promise((resolve,reject)=>{if(!file||!String(file.type||'').startsWith('image/'))return reject(new Error('ไม่พบรูปภาพ'));if(file.size>MAX_SOURCE_BYTES)return reject(new Error('รูปมีขนาดเกิน 5MB'));const r=new FileReader(),img=new Image();r.onload=()=>img.src=String(r.result||'');r.onerror=()=>reject(new Error('อ่านรูปไม่สำเร็จ'));img.onerror=()=>reject(new Error('เปิดรูปไม่สำเร็จ'));img.onload=()=>{try{const scale=Math.min(1,1100/Math.max(img.naturalWidth,img.naturalHeight)),w=Math.max(1,Math.round(img.naturalWidth*scale)),h=Math.max(1,Math.round(img.naturalHeight*scale)),c=document.createElement('canvas');c.width=w;c.height=h;const ctx=c.getContext('2d',{alpha:false});ctx.fillStyle='#fff';ctx.fillRect(0,0,w,h);ctx.drawImage(img,0,0,w,h);let q=.72,data=c.toDataURL('image/jpeg',q);while(data.length>targetBytes&&q>.18){q-=.06;data=c.toDataURL('image/jpeg',q);}if(data.length>targetBytes)throw new Error('รูปมีขนาดใหญ่เกินไป');resolve(data);}catch(e){reject(e);}};r.readAsDataURL(file);});}
async function currentImages(id){const s=await db.collection('order_status').doc(id).get();if(!s.exists)return[];const d=s.data()||{},a=Array.isArray(d.fulfillmentSlipDataList)?d.fulfillmentSlipDataList:(d.fulfillmentSlipData?[d.fulfillmentSlipData]:[]);return a.filter(x=>typeof x==='string'&&x.startsWith('data:image/')).slice(0,MAX_IMAGES);}
function paint(card,images){const panel=card.querySelector('.ymk-fulfillment-slip'),preview=panel&&panel.querySelector('.ymk-fulfillment-slip-preview'),note=panel&&panel.querySelector('.ymk-fulfillment-slip-note');if(preview){preview.innerHTML='';images.forEach((src,i)=>{const img=document.createElement('img');img.src=src;img.alt='สลิปเติมเกม '+(i+1);preview.appendChild(img);});preview.classList.toggle('show',images.length>0);}if(note)note.textContent=images.length?`แนบสลิปเติมเกมแล้ว ${images.length} รูป ✓`:`แนบกี่รูปก็ได้ตามที่ร้านต้องการ • สูงสุด ${MAX_IMAGES} รูป`;}
async function savePasted(card,newFiles,btn){if(!db||!auth?.currentUser){alert('กรุณาเข้าสู่ระบบร้านก่อนค่ะ');return;}const fresh=[...(newFiles||[])].filter(f=>String(f.type||'').startsWith('image/'));if(!fresh.length){alert('ไม่พบรูปภาพค่ะ');return;}const oldText=btn?btn.textContent:'';if(btn){btn.disabled=true;btn.textContent='กำลังบันทึก…';}try{const saved=await currentImages(card.dataset.id),space=MAX_IMAGES-saved.length,accepted=fresh.slice(0,space);if(!accepted.length){alert('สลิปครบ 30 รูปแล้วค่ะ');return;}const target=Math.max(22000,Math.min(100000,Math.floor(TOTAL_DATA_BUDGET/(saved.length+accepted.length)))),added=[];for(let i=0;i<accepted.length;i++)added.push(await compressImage(accepted[i],target));const images=[...saved,...added];await db.collection('order_status').doc(card.dataset.id).set({fulfillmentSlipAttached:true,fulfillmentSlipCount:images.length,fulfillmentSlipDataList:images,fulfillmentSlipFileNames:images.map((_,i)=>'topup-slip-'+(i+1)+'.jpg'),fulfillmentSlipData:firebase.firestore.FieldValue.delete(),fulfillmentSlipFileName:firebase.firestore.FieldValue.delete(),fulfillmentSlipUpdatedAt:firebase.firestore.FieldValue.serverTimestamp()},{merge:true});try{await db.collection('orders').doc(card.dataset.id).set({fulfillmentSlipAttached:true,fulfillmentSlipCount:images.length},{merge:true});}catch(e){}paint(card,images);}catch(e){alert('บันทึกสลิปไม่สำเร็จ: '+(e?.message||e));}finally{if(btn){btn.disabled=false;btn.textContent=oldText;}}}
async function readClipboardImages(){const items=await navigator.clipboard.read(),files=[];for(const item of items)for(const type of item.types||[])if(type.startsWith('image/')){const blob=await item.getType(type);files.push(new File([blob],'clipboard.png',{type}));break;}return files;}
function enhance(){document.querySelectorAll('#orders .card').forEach(card=>{const panel=card.querySelector('.ymk-fulfillment-slip');if(!panel||panel.dataset.pasteEnhanced)return;panel.dataset.pasteEnhanced='1';const actions=panel.querySelector('.ymk-fulfillment-slip-actions');if(!actions)return;const btn=document.createElement('button');btn.type='button';btn.className='ymk-fulfillment-paste';btn.textContent='วางรูปจากคลิปบอร์ด';actions.insertBefore(btn,actions.children[1]||null);const zone=document.createElement('div');zone.className='ymk-fulfillment-paste-zone';zone.tabIndex=0;zone.textContent='คัดลอกรูปมาแล้ว คลิกตรงนี้แล้วกด Ctrl+V ได้เลย • รูปที่วางจะเพิ่มต่อจากรูปเดิม';panel.appendChild(zone);btn.onclick=async()=>{try{await savePasted(card,await readClipboardImages(),btn);}catch(e){alert(e?.message||e);}};zone.addEventListener('paste',async e=>{const files=[];for(const item of [...(e.clipboardData?.items||[])]){if(item.kind==='file'&&String(item.type||'').startsWith('image/')){const f=item.getAsFile();if(f)files.push(f);}}if(files.length){e.preventDefault();await savePasted(card,files,btn);}});});}
const root=document.getElementById('orders');if(root)new MutationObserver(enhance).observe(root,{childList:true,subtree:true});setInterval(enhance,500);enhance();
})();