(function(){
  const baseKeys=['echoes','skins','accessories'];
  const fallback={all:'ทั้งหมด',echoes:'เติมกระดุม',skins:'เติมสกิน',accessories:'เติมประดับ'};
  let labels={...fallback},custom=[],active='all',busy=false;
  const slug=s=>'custom_'+String(s||'').trim().toLowerCase().replace(/\s+/g,'_').replace(/[^a-z0-9ก-๙_\-]/g,'').slice(0,50);
  function load(){try{Object.assign(labels,JSON.parse(localStorage.getItem('ymk_category_labels')||'{}'));custom=JSON.parse(localStorage.getItem('ymk_custom_categories')||'[]');if(!Array.isArray(custom))custom=[];}catch(e){custom=[];}custom.forEach(x=>labels[x.id]=x.name);}
  function save(){try{localStorage.setItem('ymk_category_labels',JSON.stringify(labels));localStorage.setItem('ymk_custom_categories',JSON.stringify(custom));}catch(e){}}
  function keys(){return ['all',...baseKeys,...custom.map(x=>x.id)];}
  function addOptions(){document.querySelectorAll('.p-category').forEach(sel=>{const cur=sel.value;baseKeys.forEach(id=>{const o=[...sel.options].find(x=>x.value===id);if(o)o.textContent=labels[id];});custom.forEach(x=>{let o=[...sel.options].find(v=>v.value===x.id);if(!o){o=document.createElement('option');o.value=x.id;sel.appendChild(o);}o.textContent=labels[x.id]||x.name;});if(cur&&[...sel.options].some(o=>o.value===cur))sel.value=cur;});}
  function apply(){const list=document.getElementById('productList');if(list)list.querySelectorAll('.productEdit').forEach(card=>{const cat=card.querySelector('.p-category')?.value||'';card.style.display=(active==='all'||cat===active)?'':'none';});const bar=document.getElementById('productCategoryTabs');if(bar)bar.querySelectorAll('[data-category]').forEach(b=>{const on=b.dataset.category===active;b.style.background=on?'#e889ad':'#fff0f6';b.style.color=on?'#fff':'#92566e';b.style.borderColor=on?'#e889ad':'#efc8d7';});}
  function render(){const list=document.getElementById('productList');if(!list)return;let bar=document.getElementById('productCategoryTabs');if(!bar){bar=document.createElement('div');bar.id='productCategoryTabs';bar.style.cssText='display:flex;flex-wrap:wrap;gap:8px;margin:0 0 14px;padding:6px;background:#fff;border:1px solid #f2ccdc;border-radius:16px;';list.parentNode.insertBefore(bar,list);}bar.innerHTML='';keys().forEach(key=>{const b=document.createElement('button');b.type='button';b.dataset.category=key;b.textContent=labels[key]||key;b.style.cssText='min-width:110px;height:42px;border-radius:12px;border:1px solid #efc8d7;background:#fff0f6;color:#92566e;font-weight:850;cursor:pointer;padding:0 12px;';b.onclick=()=>{active=key;apply();};bar.appendChild(b);});const add=document.createElement('button');add.type='button';add.textContent='+ สร้างหมวด';add.style.cssText='height:42px;border-radius:12px;border:1px dashed #df8eaf;background:#fff;color:#c9638d;font-weight:850;cursor:pointer;padding:0 14px;';add.onclick=createCategory;bar.appendChild(add);const edit=document.createElement('button');edit.type='button';edit.textContent='✎ แก้ชื่อหมวด';edit.style.cssText='height:42px;border-radius:12px;border:1px solid #efc8d7;background:#fff;color:#c9638d;font-weight:850;cursor:pointer;padding:0 14px;';edit.onclick=chooseRename;bar.appendChild(edit);apply();}
  function createCategory(){if(busy)return;const name=prompt('ชื่อหมวดหมู่ใหม่');if(!name||!name.trim())return;let id=slug(name);if(!id||id==='custom_')id='custom_'+Date.now();if(keys().includes(id)){alert('มีหมวดหมู่นี้แล้วค่ะ');return;}custom.push({id,name:name.trim()});labels[id]=name.trim();save();addOptions();render();active=id;apply();}
  function chooseRename(){const editable=keys().filter(x=>x!=='all');const menu=editable.map((id,i)=>(i+1)+'. '+(labels[id]||id)).join('\n');const pick=prompt('เลือกหมวดที่จะแก้ชื่อ โดยพิมพ์เลข\n\n'+menu);if(!pick)return;const idx=Number(pick)-1;if(!Number.isInteger(idx)||idx<0||idx>=editable.length){alert('เลขหมวดไม่ถูกต้องค่ะ');return;}rename(editable[idx]);}
  async function rename(id){const old=labels[id]||id;const name=prompt('ชื่อใหม่ของหมวด',old);if(!name||!name.trim()||name.trim()===old)return;labels[id]=name.trim();const c=custom.find(x=>x.id===id);if(c)c.name=name.trim();save();addOptions();render();try{if(typeof db!=='undefined'&&db&&typeof auth!=='undefined'&&auth?.currentUser){const snap=await db.collection('products').where('category','==',id).get();if(!snap.empty){const batch=db.batch();snap.docs.forEach(doc=>batch.set(doc.ref,{categoryLabel:name.trim(),updatedAt:firebase.firestore.FieldValue.serverTimestamp()},{merge:true}));await batch.commit();}}}catch(e){console.warn(e);}}
  function sync(){try{if(typeof db==='undefined'||!db)return;db.collection('products').onSnapshot(s=>{let changed=false;s.docs.forEach(d=>{const p=d.data()||{},id=String(p.category||'');if(id&&!baseKeys.includes(id)&&id!=='all'&&!custom.some(x=>x.id===id)){const n=String(p.categoryLabel||id);custom.push({id,name:n});labels[id]=n;changed=true;}});if(changed){save();render();}addOptions();apply();},()=>{});}catch(e){}}
  function start(){load();render();addOptions();document.addEventListener('change',e=>{if(e.target?.classList?.contains('p-category'))apply();});setInterval(()=>{addOptions();apply();},1200);sync();}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();
})();