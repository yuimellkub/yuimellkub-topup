(function(){
  let db=null,items=[],active=null;
  const BASE={echoes:'เติมกระดุม',skins:'เติมสกิน',accessories:'เติมประดับ'};
  function esc(v=''){return String(v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));}
  function ready(){try{if(!window.firebase||!firebase.firestore)return false;if(!firebase.apps.length){if(!window.YUIMELLKUB_FIREBASE_CONFIG)return false;firebase.initializeApp(window.YUIMELLKUB_FIREBASE_CONFIG);}db=firebase.firestore();return true;}catch(e){return false;}}
  function categoryName(id){const p=items.find(x=>x.category===id&&x.categoryLabel);return p?.categoryLabel||BASE[id]||id;}
  function host(){return document.querySelector('.ready-stock-tabs')||document.querySelector('.ready-stock-tab')?.parentElement;}
  function grid(){return document.querySelector('.ready-stock-grid');}
  function card(p){const status=p.status||'ready',label=status==='out'?'หมดชั่วคราว':status==='paused'?'ปิดชั่วคราว':'พร้อมเติม',disabled=status!=='ready';return `<article class="ready-stock-card${disabled?' is-soldout':''}" data-ymk-product="${esc(p.id)}"><div class="ready-stock-image-wrap">${p.image?`<img class="ready-stock-image" src="${esc(p.image)}" alt="${esc(p.name||'สินค้า')}" style="object-position:center ${esc(p.imagePosition||'center')}">`:''}</div><div class="ready-stock-info"><div class="ready-stock-name">${esc(p.name||'สินค้า')}</div><span class="ready-stock-status ${disabled?'is-out':'is-ready'}">${label}</span><div class="ready-stock-description">${esc(p.description||'พร้อมเติมทันที')}</div><div class="ready-stock-bottom"><strong class="ready-stock-price">${Number(p.price||0)} บาท</strong><button type="button" class="ready-stock-order-btn" data-ready-name="${esc(p.name||'สินค้า')}" data-ready-price="${Number(p.price||0)}" data-ready-category="${esc(p.category||'echoes')}" ${disabled?'disabled':''}>${disabled?label:'สั่งซื้อ'}</button></div></div></article>`;}
  function renderTabs(){const h=host();if(!h)return;const cats=[];items.filter(x=>x.visible!==false).forEach(p=>{const c=p.category||'echoes';if(!cats.includes(c))cats.push(c);});['echoes','skins','accessories'].forEach(c=>{if(!cats.includes(c))cats.push(c);});if(!active||!cats.includes(active))active=cats[0]||'echoes';h.innerHTML='';cats.forEach(c=>{const b=document.createElement('button');b.type='button';b.className='ready-stock-tab'+(c===active?' active':'');b.dataset.category=c;b.textContent=categoryName(c);b.addEventListener('click',()=>{active=c;renderTabs();renderProducts();});h.appendChild(b);});}
  function renderProducts(){const g=grid();if(!g)return;const rows=items.filter(p=>p.visible!==false&&(p.category||'echoes')===active).sort((a,b)=>(a.order||0)-(b.order||0));if(!rows.length){g.innerHTML='<div style="padding:28px;text-align:center;color:#aa7c8e">ยังไม่มีสินค้าในหมวดนี้ ♡</div>';return;}g.innerHTML=rows.map(card).join('');document.dispatchEvent(new CustomEvent('ymk-storefront-products-rendered'));}
  function start(){if(!ready())return setTimeout(start,250);db.collection('products').onSnapshot(s=>{items=s.docs.map(d=>({id:d.id,...d.data()}));renderTabs();renderProducts();},e=>console.warn('storefront category sync failed',e));}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();
})();